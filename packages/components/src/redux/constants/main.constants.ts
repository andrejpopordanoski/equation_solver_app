import { Action } from '../core/Action';

export const GET_RANDOM = { entity: 'GET_RANDOM', action: Action.HANDLE };
// created with createNewAction.sh
export const CROP_IMAGE = {
    entity: 'CROP_IMAGE',
    action: Action.HANDLE,
};
// created with createNewAction.sh
export const MER_PREDICT = {
    entity: 'MER_PREDICT',
    action: Action.HANDLE,
};
