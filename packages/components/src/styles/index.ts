import { colors } from './colors';
import { basicStyles, headers } from './basic';
// import { textSizeStyles } from './sizes';
import { globalStyles } from './global';

export { colors, basicStyles, headers, globalStyles };
